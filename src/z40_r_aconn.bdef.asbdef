managed implementation in class ZBP40_R_ACONN unique;
strict ( 2 );
with draft;
extensible;
define behavior for Z40_R_ACONN alias Verbindungen
persistent table Z40ACONN
extensible
draft table Z40_D_ACONN
etag master LocalLastChangedAt
lock master total etag LastChangedAt
authorization master( global )
{
  field ( readonly )
   UUID,
   LocalCreatedBy,
   LocalCreatedAt,
   LocalLastChangedBy,
   LocalLastChangedAt,
   LastChangedAt;

  field ( numbering : managed )
   UUID;

  field ( readonly )
     CityFrom,
     CountryFrom,
     CityTo,
     CountryTo;


  create;
  update;
  delete;

 validation CheckSemanticKey on save { create; update; } // Hier die validierung mit Warnung Aktivieren dann af Checksem strg + 1
 determination GetCities on save { field AirportFromID, AirportToID; }

  draft action Activate optimized;
  draft action Discard;
  draft action Edit;
  draft action Resume;
  draft determine action Prepare;

  mapping for Z40ACONN corresponding extensible
  {
    UUID = UUID;
    CarrierID = CARRIER_ID;
    ConnectionID = CONNECTION_ID;
    AirportFromID = AIRPORT_FROM_ID;
    CityFrom = CITY_FROM;
    CountryFrom = COUNTRY_FROM;
    AirportToID = AIRPORT_TO_ID;
    CityTo = CITY_TO;
    CountryTo = COUNTRY_TO;
    LocalCreatedBy = LOCAL_CREATED_BY;
    LocalCreatedAt = LOCAL_CREATED_AT;
    LocalLastChangedBy = LOCAL_LAST_CHANGED_BY;
    LocalLastChangedAt = LOCAL_LAST_CHANGED_AT;
    LastChangedAt = LAST_CHANGED_AT;
  }

}